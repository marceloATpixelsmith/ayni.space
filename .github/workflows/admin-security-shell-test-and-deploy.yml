name: Admin Frontend Tests

on:
  pull_request:
    branches: [master]
    paths:
      - "apps/admin/**"

permissions:
  contents: read

jobs:
  admin-security-shell-test:
    name: admin-security-shell-test
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - run: pnpm install --frozen-lockfile

      - name: Run admin security shell contract test
        run: pnpm --filter @workspace/admin run test:security-shell

      - name: Run admin runtime vitest suite
        run: pnpm --filter @workspace/admin test
