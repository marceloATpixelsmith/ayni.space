name: Auth Security Regression Suite

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
  schedule:
    - cron: "0 3 * * *"

permissions:
  contents: read

jobs:
  auth-security-regression-suite:
    name: auth-security-regression-suite
    runs-on: ubuntu-latest
    env:
      CI: "true"
      BACKEND_TRACE_VERBOSE: "0"
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - name: Reset pnpm store before install
        run: |
          STORE_PATH="$(pnpm store path --silent)"
          rm -rf "$STORE_PATH"
          mkdir -p "$STORE_PATH"
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run auth-security-regression-suite
        run: |
          mkdir -p ci-output
          LOG_FILE="ci-output/auth-security-regression-suite.log"
          COMMAND="pnpm run test:auth-security-regression"
          {
            echo "Failing command: $COMMAND"
            echo "----------------------------------------"
          } > "$LOG_FILE"
          set +e
          $COMMAND 2>&1 | tee -a "$LOG_FILE"
          STATUS=${PIPESTATUS[0]}
          set -e
          echo "----------------------------------------" >> "$LOG_FILE"
          echo "Exit code: $STATUS" >> "$LOG_FILE"
          if [ "$STATUS" -ne 0 ]; then
            node scripts/ci/extract-failure-summary.mjs "$LOG_FILE" | tee -a "$GITHUB_STEP_SUMMARY"
            exit "$STATUS"
          fi
      - name: Upload auth-security-regression-suite failure log
        if: ${{ failure() && hashFiles('ci-output/auth-security-regression-suite.log') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: auth-security-regression-suite-failure-log
          path: ci-output/auth-security-regression-suite.log
          if-no-files-found: error
