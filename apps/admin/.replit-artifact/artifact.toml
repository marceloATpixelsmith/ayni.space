kind = "web"
previewPath = "/"
title = "SaaS Platform"
version = "1.0.0"
id = "apps/admin"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 23633
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/admin run dev"

[services.env]
PORT = "23633"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/admin", "run", "build"]
publicDir = "apps/admin/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
